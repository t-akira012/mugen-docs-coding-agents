# AGENTS.ja.md

## 目的

このリポジトリは、Elecbyte M.U.G.E.N のキャラクターコーディング仕様を、Coding Agent がローカルで参照できる生成済みコーパスとして提供します。
主用途は、WinMUGEN 向けキャラクターの CNS / CMD / AIR コードを編集する際に、生成済みドキュメントから仕様を確認することです。

このファイルは、**生成済みコーパスの利用方法**だけを定めます。生成・再生成・依存関係導入・保守手順は対象外です。

## 必須の参照手順

エンジン挙動、State Controller の構文、Trigger の意味、既定値、評価タイミング、実行コンテキストを確認する場合は、必ず以下の順序で参照します。

1. まず `generated/index.json` を検索する。
2. 対象となる Controller、Trigger、CNS 見出し、AIR 見出しを正確に特定する。
3. 索引エントリが指す Markdown ファイルを開く。
4. 対象セクションと、解決に直接必要な関連セクションだけを読む。
5. `docs/` は、手書きの運用ノートおよび互換性境界の確認に使用する。
6. 既存キャラクターコードは、プロジェクト固有規約の確認にのみ使用し、エンジン仕様の証明には使わない。

ローカルコーパスに該当仕様が存在する場合、`generated/index.json` を飛ばして記憶や推測だけで判断してはいけません。

## ローカル文書のみを使用する規則

キャラクターコーディング時は、このリポジトリ内に存在する資料だけを使用します。

- Elecbyte のウェブ文書を取得しない。
- ドキュメント生成器を実行しない。
- コーパスを読むために依存関係の導入・同期を行わない。
- リポジトリの生成・保守手順を、キャラクターコーディング作業の一部として扱わない。

必要な挙動をローカルコーパスだけでは確定できない場合、外部仕様を暗黙に持ち込まず、未解決または `unverified` として報告します。

## 互換性規則

生成済みコーパスには M.U.G.E.N 1.0 の公式資料に由来する内容が含まれます。M.U.G.E.N 1.0 に記載されているという理由だけで、旧 WinMUGEN に存在する機能だと推論してはいけません。

WinMUGEN 厳密互換で作業する場合:

- M.U.G.E.N 1.1 または Ikemen GO の拡張機能を使用しない。
- WinMUGEN 互換と扱うのは、ローカルコーパス、`docs/COMPATIBILITY.md`、対象ランタイム、または対象キャラクターの証拠から互換性を確認できた場合のみとする。
- 確認できない場合は `unverified` とする。

## コーディング規則

- State Controller の順序を維持する。並べ替えによって挙動が変わり得る。
- 同一番号の `triggerN` を維持する。同じ番号の行は AND グループを構成し、異なる番号のグループ同士は代替条件になる。
- 意味を確定せず、欠番をまたいで Trigger 番号を振り直さない。
- `StateDef`、`ChangeState`、`HitDef`、`AnimElem`、`Time`、`triggerall`、`persistent`、`ignorehitpause` などのエンジン識別子を翻訳しない。
- State Controller のパラメータ、Trigger 名、既定値、戻り値を捏造しない。
- コメントと実行可能な CNS 構文を分離する。`;` 以降はコメントである。
- State -3、-2、-1、および現在 State を別々の実行コンテキストとして扱う。
- Helper の実行規則と Root Player の実行規則を分離して扱う。

## キャラクターコード編集手順

Controller または Trigger を変更する前に、以下を行います。

1. 対象となるエンジン構文を正確に特定する。
2. `generated/index.json` から対応項目を探す。
3. 必須・任意パラメータ、既定値、合法な値を確認する。
4. 実行コンテキストと Trigger の評価タイミングを確認する。
5. バージョン依存挙動の場合は WinMUGEN 互換性を確認する。
6. 要求を満たす最小限のコード変更のみを行う。

動作変更と無関係な整形、State Controller の並べ替え、広範囲なクリーンアップを同時に行ってはいけません。
